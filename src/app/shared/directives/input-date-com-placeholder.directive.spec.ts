import { InputDateComPlaceholderDirective } from './input-date-com-placeholder.directive';

describe('InputDateComPlaceholderDirective', () => {
  it('should create an instance', () => {
    const directive = new InputDateComPlaceholderDirective();
    expect(directive).toBeTruthy();
  });
});
